extends layout

block content
  h1= title
  p Example of a scatter, contour and stream graph, all representing the same data (i.e. a D3 port of <a href="http://selwyn-mccracken.com/uk_coffee_brand_landscape">http://selwyn-mccracken.com/uk_coffee_brand_landscape</a>)
  style
    .node {
      stroke: #fff;
      stroke-width: 1.5px;
    }

    .link {
      stroke: #999;
      stroke-opacity: .6;
    }
    .axis text {
      font: 10px sans-serif;
    }

    .axis path,
    .axis line {
      fill: none;
      stroke: #000;
      shape-rendering: crispEdges;
    }

    .axis path {
      display: none;
    }

    .isoline {
      fill: none;
      stroke: #fff;
    }
  svg#chart
  script(type='text/javascript', src='/javascripts/bootstraps/scatter.js')